import { PageShell } from './PageShell';

export function Contact() {
  return (
    <PageShell eyebrow="Contact" title="Send a signal">
      <p>
        Add email, social links, scheduling preferences, and a short note about what kinds of
        conversations or collaborations are welcome.
      </p>
    </PageShell>
  );
}
